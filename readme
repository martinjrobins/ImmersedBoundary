This user project is NO LONGER the location of the Chaste Immersed Boundary
method implementation.

It has been migrated to the Git repo.

Users must checkout the feature branch 'origin/fcooper/immersed_boundary',
where the source is now merged into `cell_based`, `mesh`, and `utilities`.
